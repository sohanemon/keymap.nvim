{
  "name": "keymap.nvim",
  "version": "1.0.0",
  "description": "A lightweight Neovim plugin for creating key mappings with optional which-key integration",
  "scripts": {
    "test": "lua tests/run_tests.lua",
    "test:watch": "while true; do lua tests/run_tests.lua; sleep 2; clear; done",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^9.1.0"
  }
}
